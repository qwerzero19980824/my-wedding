# CHANGELOG — 平行宇宙的相遇

## v3.29.1 — 轮换照片说明支持换行 (2026-07-17)

- 轮换照片的聚焦字幕与原图预览说明使用 `pre-wrap` 保留回车换行，并兼容长文本自动折行。
- 手机端可直接按回车分行；编辑读取改用 `innerText`，避免 `contenteditable` 生成的分行节点被拼成一行。
- Worker 字幕清洗保留安全换行，云端保存及手机、电脑读取不会再丢失行结构。
- 正式 Worker 已发布为 `a8d60b91-91c4-43b4-a4c3-99cf2e85e2c0`。

## v3.29.0 — 稳定拖动 · 双结尾照片 · 云端歌单 (2026-07-17)

- 平行世界文字拖动改为触摸手势锁定、`requestAnimationFrame` 合帧更新、松手后一次保存；移动端不再与页面滚动争抢，文字限制在各自轨道可视范围内。
- 结尾改为两张独立 R2 照片：第一张在回答后浮现，点击第一张再显示第二张；旧单图记录自动兼容。
- 编辑工具支持批量上传背景音乐到 R2，歌单加入全站云快照，手机与电脑读取同一歌单；支持自动续播和手动下一首。
- 路线页增加高德中国静态地图代理背景，Web Service Key 只保存在 Cloudflare Secret；未配置时保留现有中国轮廓兜底。
- Worker 站点快照白名单扩展到 13 项，并新增音乐歌单双向同步回归。
- 正式 Worker 已发布为 `118a8014-d54c-49e2-9b28-729dc01becbc`；高德 Key 尚未配置时接口返回 503，页面继续使用内置中国轮廓。

## v3.28.1 — 双向同步确认提示 (2026-07-17)

- 编辑工具“保存”会等待云端确认：成功明确提示“已同步到所有设备”，失败明确提示仅保存在本机。
- Worker 测试新增手机写入→电脑读取→电脑补充→手机读回的双向回归，覆盖文字与云端图片地址。

## v3.28.0 — 全站多端云同步 · 清晰照片资产 (2026-07-17)

- 新增 R2 `/api/site-state`：全站文字、文字位置、故事模块、自由框、五个主叙事照片位、28 张拍立得及背面故事、页面隐藏状态和编辑样式统一保存为云端站点快照；本机存储仅作为断网副本。
- 新增 R2 `/api/assets/:id` 与公开媒体读取；手机新上传的主叙事、故事模块、自由框和拍立得照片会生成最长边 1800px、JPEG 0.88 的清晰云端版本，快照只保存地址，不再受 4.5MB Base64 容量限制。
- 云端快照使用更新时间标记：另一台设备打开或回到页面时发现新版本会安全清理并应用内容，然后自动刷新一次。
- 手机相册图片识别兼容 MIME 缺失及 HEIC/HEIF 文件名；桌面 1280px 与手机 390×844 验证均无横向溢出和脚本错误。
- Worker 测试覆盖站点快照鉴权、字段白名单、格式拒绝以及云端图片资产上传、读取、缓存与删除。
- 正式 Worker 已发布为 `3c39c01d-acc8-482d-9d47-36a2f1992bcb`；管理员 Secret、正式域名 CORS、正确/错误口令及临时图片完整生命周期均已线上验证。
- 仓库根目录新增 `.nojekyll`，GitHub Pages 直接发布静态站点，不再把 Markdown 示例中的 `{{ ... }}` 当作 Liquid 模板解析。

## v3.26.0 — 平行世界叙事扩展 · 验证超时提示 (2026-07-16)

- 平行世界左右轨道改为完全同速滚动；叙事段落延长，承载更完整的双线故事。
- 原有双轨文本位扩展为左右各 10 个、共 20 个；可在编辑模式直接填写，单段可删除，空段仅在编辑模式出现，避免影响正式展示。
- 管理员口令验证新增 12 秒连接超时提示，不再长时间停在“正在验证”；口令从不写入本地存储。

## v3.25.1 — 结尾照片上传修复 (2026-07-16)

- 修复结尾照片使用不符合 Worker 校验规则的 `finale-photo` 编号，导致即使管理员口令正确也会收到 `400 invalid-id` 的问题；现统一使用保留 ID `poster-finale-photo`。
- 上传失败提示按实际原因区分：管理员口令、文件过大、请求格式、云端配置和网络/服务异常不再混为“口令错误”。
- Worker 自动化测试覆盖旧 `finale-photo` 被拒绝与正式 `poster-finale-photo` 完整生命周期，避免编号契约再次漂移。

## v3.25.0 — 恋爱天数 · 结尾照片 · 发布收口 (2026-07-16)

### 体验与内容
- 首屏新增恋爱天数计数器，按 2018-07-20 起始日自动计算；仅编辑模式可调整字号、字重、颜色与位置，样式保存到 `wedding_anniversary_style_v1`。
- 故事海报调整为居中的 16:9 相框，竖图使用柔焦填充背景、横图保持完整主体；手机端加载与间距同步优化。
- 最终求婚章节支持管理员上传一张结尾照片，照片保存在既有 R2 图库的固定保留记录中，不进入内容包。

### 性能与发布安全
- 正文打开或页面切到后台时，WebGL 网纱停止执行物理计算和渲染；回到首屏后仍由既有 `resetSim()` 重建布料。
- `.vscode/` 与本地 UI/UX 参考库加入 `.gitignore`；发布流程改为只暂存已核对的项目文件，避免 `git add .` 误提交参考资料或编辑器设置。
- README、CHECKPOINT、项目总控/响应式技能、图片存储与部署说明同步至 v3.25.0。

## v3.24.0 — 单框连续密码 · 七图 Coverflow · 页面删改 (2026-07-14)

### 私密入口与主人认证
- 故事入口改为单一密码框；界面只显示“娜娜の生日”，不显示第几次或进度提示。正确输入 `19980607` 后静默清空，连续第二次正确才进入；任何错误都会清零且只提示“密码错误”。
- 主人认证区分口令错误、来源未放行和服务不可达；Worker 配置新增 `localhost:8080` / `127.0.0.1:8080` CORS，并保留 8090 调试地址。

### Coverflow、相纸与页面管理
- Coverflow 改为中心 1 张加左右各 3 张，共 7 张可见；中心保持原亮度，全部侧卡使用统一 0.42 黑色遮罩，并缩小间距/纵深保证手机端仍可见。
- 放大的相纸在点击其他位置后自动退出放大，但照片正反面、背面文字和排列状态保持不变。
- 编辑工具新增“删除当前页”和“恢复删页”；删除采用可恢复隐藏，开场入口不可删除，状态保存到 `wedding_hidden_pages_v1` 并随内容包导入导出。

### 验证与部署状态
- Worker Mock R2 测试、内联脚本解析和 `git diff --check` 通过；测试新增 `localhost:8080` 的认证/CORS 覆盖。
- In-app Browser 实测：单密码框错误提示、两次连续正确进入、桌面和 390×844 均显示 7 张、侧卡统一变暗、相纸点外部取消放大且翻面保留、删除/恢复页面成功。
- Worker 已发布为版本 `aa1960c6-87f0-4a1b-8113-4a6470d15711`；真实验证 `localhost:8080` 与 `127.0.0.1:8080` 预检均为 204，正确管理口令均为 200，错误口令为 401。本轮未推送 GitHub。

## v3.23.0 — Coverflow Gallery · 双密码入口 · 主人编辑认证 (2026-07-13)

### Coverflow Gallery
- 按 `Coverflow Gallery.md` 重做故事照片轮换：中心卡直立，左右各保留两张，使用 1600px 透视、9° Y 轴倾斜、10° Z 轴倾斜、240px 纵深和 0.6 秒缓动。
- 移除旧的星空自动旋转与强金色定格，改为静止画廊；支持点侧卡、拖动、方向键和左右按钮切换，中心照片可点开原图与字幕。
- 保留本机 IndexedDB、R2 原图/缩略图、50–80 张以上批量上传和逐图字幕编辑；仅中心附近 5 张进入可见/加载范围。

### 私密入口与主人权限
- “翻开我们的故事”新增双密码窗口；提示为“娜娜の生日”，只有两次都输入 `19980607` 才进入，只正确一次仍显示密码错误。
- 编辑模式不再由公开铅笔直接开启；必须先用 R2 管理口令调用 Worker `/api/auth` 验证，成功后本标签页才显示上传与编辑工具。
- 管理口令只保留在内存，不写入 localStorage、源码或 Git；Worker 继续在服务端对上传、字幕修改和删除执行 Bearer 校验。

### UI/UX 与配置
- 引入基础值、语义、组件三层 CSS 设计令牌，统一新弹窗、Coverflow 控件、焦点、错误、加载和触控状态。
- 密码/管理员弹窗支持焦点恢复、Escape、遮罩关闭、Tab 焦点环与 `aria-live` 错误提示；390×844 下不横向溢出。
- 读取线上已保存的首屏配置并将 `tiling` 默认值从 448 写为 450；允许浏览器缩放，并补齐减少动态效果。

### 验证
- `index.html` 内联脚本解析、Worker 语法、`git diff --check` 通过；Worker Mock R2 测试覆盖 `/api/auth` 的 401/200。
- In-app Browser 验证双密码错误/成功分支、Coverflow 五卡布局与按钮切换、停止自动轮转、390×844 无溢出、错误管理口令拒绝和正确口令手机解锁。
- Worker 已部署为版本 `a114bb08-7b6b-4945-817c-d54c12874df6`；本轮未推送 GitHub。

## v3.22.0 — R2 正式部署 · 跨设备图库上线 (2026-07-13)

### Cloudflare 上线
- 创建私有 R2 bucket `wedding-posters`，部署 Worker `my-wedding-poster-library`，正式地址为 `https://my-wedding-poster-library.yuyanp52.workers.dev`。
- `UPLOAD_TOKEN` 使用 256 位随机值保存为 Cloudflare Worker Secret；本机恢复副本位于被 Git 忽略的 `cloudflare-r2-worker/.upload-token.local.txt`，不进入源码或内容包。
- 正式 CORS 同时允许 `www.cml-zy.love`、裸域名以及 8090 本地调试地址；固定启用 workers.dev 并关闭无用预览 URL。

### 前端接入
- 新设备第一次打开页面时默认连接正式 Worker；管理员仍可在编辑模式中切换云端地址或明确关闭云端模式。
- 云端原图、960px 缩略图和字幕目录跨设备共享；上传口令仍只保留在当前标签页内存。

### 线上验证
- 真实 R2 生命周期测试通过：健康检查 200、预检 204、未授权上传 401、原图/缩略图上传 200、目录创建 201、公开清单、字幕 PATCH、媒体读取和删除均成功。
- 测试对象删除后目录恢复为空；本地 Worker 自动测试、内联脚本解析与 `git diff --check` 继续通过。

## v3.21.1 — 触碰即时放大 · 批量上传容错 (2026-07-13)

### 海报触碰体验
- 当前中心海报在鼠标点击或手指轻触后立即进入放大状态，不再等待 520ms 定格动画；点击侧边海报时仅用 280ms 快速对齐后放大。
- 统一点击与触碰结束逻辑，并把重复点击抑制窗口延长到 360ms，避免部分手机同时触发 `pointerup` 与延迟 `click` 后出现“刚放大就复原”。

### 大批量云端上传
- 80 张或更多照片仍逐张上传原图、960px 缩略图和目录信息；单张损坏或网络失败不再中断整批。
- 失败照片会执行尽力清理，整批结束后明确显示成功与失败数量；管理口令错误仍立即终止，避免反复弹出口令框。

### 验证
- In-app Browser 实测桌面与 390px 手机端轻触后 90ms 内进入放大状态，白色字幕保持显示；手机横向拖动可退出放大并继续轮换。
- 390px 下 `scrollWidth < innerWidth`，无新增横向溢出；编辑模式的多选上传、云端设置、14–38px 字号滑块和可编辑字幕均正常。
- 内联脚本与 Worker 语法检查、`git diff --check` 通过。

## v3.21.0 — R2 云端图库适配 · 跨设备 4K 原图 (2026-07-12)

### 云端架构
- 新增 `cloudflare-r2-worker/`：使用私有 R2 binding 保存原图、缩略图和海报目录，提供上传、清单、媒体读取、字幕更新与删除接口。
- 上传、改字幕和删除使用 Worker secret `UPLOAD_TOKEN` 的 Bearer 校验；口令由用户在页面中输入，只保留在当前标签页内存，不写入 localStorage、源码或 Git。
- CORS 仅允许 `ALLOWED_ORIGINS` 中的婚礼域名和本地调试地址；原图默认单张上限 80MB，可通过 Worker 环境变量调整。

### 前端适配
- 编辑模式新增“云端图库设置”，可保存 HTTPS Worker 地址；未配置时继续使用现有 IndexedDB 本机图库。
- 云端模式下，批量选择的每张照片依次直传原图和 960px 缩略图，再写入目录；页面公开读取缩略图，点按放大时才请求原图。
- 云端、本机和故事模块照片可以共同进入轮换；云端字幕可从放大后的白色字幕区直接更新。

### 验证
- `index.html` 与 Worker 脚本语法检查通过，`git diff --check` 通过。
- 使用内存 Mock R2 完成原图上传、缩略图上传、目录写入/读取、字幕修改、媒体读取和未授权拒绝集成测试。
- 未配置 Worker 地址时本机图库路径保持不变；实际部署仍需用户创建 Cloudflare bucket、设置 secret 并授权 Worker。

## v3.20.0 — 无边界海报舞台 · 点按放大 · 本机 4K 图片库 (2026-07-12)

### 海报交互
- 海报区移除可见边框、圆角和盒状阴影，深色星空背景向视口两侧延伸，保留页面无横向溢出。
- 取消自动定格等待；海报持续轮转，只有鼠标点击或手指点按海报才会减速对准并放大，再次点按、拖动或方向键操作即可退出。
- 放大后在海报下方显示白色字幕；编辑模式可直接修改字幕，并用 14–38px 滑块调整字号，保存到 `wedding_story_poster_config_v1`。
- 用户首次拖动或点按后启用短促机械刻度音；每跨过一个中心位置播放一次，并在支持 Vibration API 的设备上同步 8ms 震动。iOS Safari 不向普通网页开放 Taptic Engine，仍保留声音反馈。

### 大容量图片库
- 编辑模式新增多选上传入口，可一次选择 80 张或更多照片。
- 4K 原图以 Blob 保存到 IndexedDB `wedding_story_poster_library_v1`；轮换只加载独立 960px JPEG 缩略图，避免同时解码几十张原图。
- 图片节点采用 12 张视窗虚拟化：仅中心附近的海报设置缩略图 `src`，离开舞台即释放；点按本机图库海报时才临时载入该张 4K 原图，退出放大后立即恢复缩略图并回收对象 URL。
- 上传前使用 `navigator.storage.estimate()` 检查容量，并请求 `navigator.storage.persist()` 降低自动清理风险；空间不足时明确拒绝本批写入。
- 本机图片库不会被塞入 localStorage 内容包，也不会自动同步到 GitHub。跨设备或公开访问应使用 R2/S3 对象存储、CDN 和服务端签名上传，架构说明见 `PHOTO_STORAGE.md`。

### 验证
- In-app Browser 验证点按放大、白色字幕、3 秒后保持放大、编辑字幕、20→30px 字号联动与恢复、手机拖动退出放大。
- 390px 下编辑控件单列、图片 input 支持 `multiple`；桌面与手机均 `scrollWidth === clientWidth`。
- IndexedDB 图片库成功打开并返回容量状态；页面控制台无运行错误，内联脚本语法与 `git diff --check` 通过。

## v3.19.0 — 十二海报弧形轮转 · 减速定格 (2026-07-12)

### 线性叙事海报舞台
- 移除通用 Swiper Coverflow 依赖，改为原生 `requestAnimationFrame` 驱动的连续立体轮转；每张海报的位置、角度、缩放、明暗和纵深都由它与中心位置的距离实时计算。
- 按参考视频重做节奏：快速连续轮转 → 平滑四次方减速 → 中心海报定格放大 → 金色光晕增强，同时压暗外围海报和四周画面。
- 照片不足 12 张时循环已有章节补齐 12 个舞台位置；达到 12 张以上后直接使用全部照片，因此后续 50 张照片可以完整进入轮转队列，屏幕只绘制中心附近的海报。
- 故事海报上传改用最长边 720px、约 34KB 的多轮降质与降尺寸压缩，50 张照片预计仍可留在现有本地内容容量保护范围内；拍立得等其他图片继续沿用原有画质规则。
- 新增鼠标/触摸拖动、左右按钮和方向键控制；拖动结束自动吸附到最近海报，`prefers-reduced-motion` 下停用自动轮转。
- 针对手机重算海报宽度、弧形跨度、透视和纵深，不把桌面布局简单缩小。

### 验证
- `index.html` 内联脚本语法检查与 `git diff --check` 通过，Swiper 运行依赖和旧回退逻辑已清除。
- In-app Browser 实测桌面轮转、减速、定格光晕和下一张按钮；390px 下拖动从 `05 / 12` 吸附到 `07 / 12`，控制台无错误。
- 390/430/768/1024/1440 宽度检查均保持 12 个舞台位置，页面 `scrollWidth === clientWidth`，无新增横向溢出。

## v3.17.0 — 相纸双 Toggle · 十章线性长卷 · 爱意地图揭示 (2026-07-11)

### 相纸交互
- 单张相纸角落操作改为“翻转 / 回忆”文字按钮，使用约 35×22px 的小型圆角矩形，不再依赖抽象图标。
- “随机摆放 / 排列摆放”合并为一个 toggle：首次随机、再次排列、第三次生成另一套随机构图。
- “一键翻面 / 一键复原”合并为一个 toggle，并同步更新按钮文字。
- 最大悬停放大倍率由旧设置的 1.6–1.8 收紧为 1.45，配合字体抗锯齿、几何精度渲染与背面缓存优化，减轻放大文字模糊。

### 线性叙事与地图
- 新增六段主线导航：初次相遇、慢慢靠近、成为我们、一起远行、平凡日常、走向余生。
- 空白项目自动获得 10 个可编辑章节，其中 6 个为大照片故事页；已有故事内容保持不变。
- 8 个地点全部点亮后，除玫瑰金夜空外，新增心跳符号与“走过万水千山，最想抵达的地方，始终是你身边”爱意揭示。

### 验证
- 浏览器实测布局 toggle 为随机 → 排列 → 新随机，一键翻面 toggle 为 28 张背面 → 0 张背面。
- 文字按钮实测约 34.5×21.8px、5px 圆角；新项目实测 10 章 / 6 个照片章节且无横向溢出。
- 地图完成态实测 8/8 地点、爱意卡片最终 `opacity:1`；页面无运行错误。

## v3.16.0 — 线性回忆长卷 · 相纸真随机 · 浪漫地图终章 (2026-07-11)

### 叙事分工
- 故事模块改为单列章节时间轴，用 `01 / 02 / 03…` 串起重点照片与完整背景故事，承担“我们怎样一路走到今天”的线性叙事。
- 28 张相纸继续承担可随手探索的瞬间档案：短便签、翻面与进入单张回忆不会与主线章节重复。

### 相纸与地图
- 正式展示态恢复“随机散落”入口；每次点击都会重新打乱 28 个安全位置、生成轻微随机旋转并保存本次构图，刷新后保持不变。
- 单张相纸的“翻转 / 进入回忆”圆形按钮由 28–36px 收紧到约 22–28px，减少对照片画面的遮挡。
- 8 个旅行地点全部点亮后，地图背景从冷色路线图柔和过渡到玫瑰金夜空，并显现星点与环形暖光。

### 验证
- 桌面浏览器验证随机布局会改变并保持散落态；按钮实测约 23px；故事区保持单列、章节轨道与大图位，无横向溢出。
- 逐个键盘访问 8 个地点后，`is-map-complete` 正确触发，玫瑰金渐变与双层浪漫光效生效。
- `git diff --check` 通过。

## v3.15.0 — 正式展示态主题收口 · 技术控件退场 (2026-07-11)

### 求婚叙事化

- 故事模块区默认文案从“Story Studio / 如何编辑”改为 `OUR CHAPTERS` 与“后来，我们把平凡的日子写成了故事”，正式页面不再向女友展示后台使用说明。
- 对已经保存过旧默认文案的浏览器增加精准迁移：仅完全等于旧默认值时更新，用户亲自修改过的文字不会被覆盖。
- 没有动态故事模块时，编辑提示空状态只在编辑模式出现；浏览态仍保留完整浪漫章节引言。

### 技术控件退场

- 地图“地名字号”滑块只在编辑模式显示，正式浏览只保留地点发现、路线流动与地图浮现。
- 相纸排列、随意摆放、间距、放大倍数、背面字号及三个保存按钮全部收回编辑模式。
- 正式照片墙只保留两个符合回忆语境的动作：“翻开所有背面”与“回到照片正面”，按钮改为更轻的圆角纸面风格。

### 验证

- 旧存储浏览器实测完成默认文案迁移，得到新 kicker、标题与浪漫正文。
- 浏览态 8 个相纸技术控件均 `display:none`、地图调节 `display:none`；编辑态全部恢复且地图调节为 `inline-flex`。
- 现场预演直达照片墙后 `top=0`，正式可见控制仅两项，页面无横向溢出。
- `index.html` 内联脚本语法检查与 `git diff --check` 通过。

## v3.14.0 — 求婚现场预演 · 章节快速定位 · 全屏入口 (2026-07-11)

### 新增

- 编辑工具栏新增“现场预演”，打开独立香槟金深色对话框；正式浏览页面不会显示这套排练控制。
- 可选择从婚纱帘幕、照片墙、“下一站”或最终求婚开始，系统先保存内容、退出编辑，再切换到纯展示状态并平滑定位。
- 新增“进入全屏展示”按钮；浏览器不支持或拒绝全屏时给出 F11 兜底提示。
- 对话框说明明确“不会修改任何文字或照片”，降低排练时误操作焦虑。

### 可访问性与响应式

- 对话框使用 `role=dialog`、`aria-modal` / `aria-hidden`，打开后聚焦第一个章节，关闭或按 Escape 后焦点返回“现场预演”。
- 点击遮罩或关闭按钮也可退出，所有章节入口均为原生按钮。
- 390×844 下对话框宽约 366px，四个章节按钮单列约 316×47px，完整落在视口内且无横向溢出。

### 验证

- In-app Browser 完整验证：编辑模式 → 现场预演 → “下一站” → 自动退出编辑、激活主内容、隐藏面板并最终定位 `targetTop=0`。
- “从婚纱帘幕开始”验证：主内容关闭、视频/DOM/WebGL 恢复、页面回到禁止滚动的首屏状态。
- 手机端对话框、Escape 关闭、焦点恢复与 ARIA 状态通过；全屏按钮因浏览器权限提示未在自动化中实际触发。
- `index.html` 内联脚本语法检查与 `git diff --check` 通过。

## v3.13.0 — 自由照片框直传 · 编辑工具可收起 (2026-07-11)

### 新增

- 工具栏“加照片框”创建的自由浮层正式支持上传与更换真实图片；照片说明仍可单独编辑。
- 图片复用 900px / 约 96KB 压缩流程，保存到原有 `wedding_free_items_v1`，并随内容包导出/导入。
- 导入时只接受安全图片 Data URL；上传前进行 4.5MB 整体容量预估，失败恢复原图片。
- 编辑工具栏新增“收起编辑工具 / 展开编辑工具”，新增自由文本框或照片框后自动收起，为拖动、配色、复制和删除控制让出画布。

### 展示与响应式保护

- 未上传图片的空自由照片框只在编辑模式出现，正式浏览时自动隐藏，避免求婚现场露出占位文案。
- 桌面创建的自由框在窗口缩窄或设备旋转后，会按框体宽度重新约束到视口内并保存新位置。
- 390px 下 280px 自由照片框自动落在约 `94–374px` 范围内，无横向溢出。
- 编辑模式下 WebGL 婚纱只保留视觉、不再拦截首屏自由框控制；退出编辑后恢复掀纱交互。

### 验证

- In-app Browser 实际新增自由照片框：编辑态上传入口 `aria-disabled=false` / `tabIndex=0`，浏览态变为不可点击不可聚焦。
- 空框浏览态 `visibility:hidden` / `opacity:0`，重新进入编辑后恢复；手机端窗口变化后框体正确归位。
- 工具栏收起后宽约 110px，自由框删除控制不再被遮挡；两次测试空框均成功清理，最终未残留测试照片框。
- `index.html` 内联脚本语法检查与 `git diff --check` 通过。

## v3.12.0 — 动态照片故事模块直传 (2026-07-11)

### 新增

- 编辑工具栏“加照片”创建的动态故事模块不再是文字占位：每个模块都可独立上传与更换真实照片。
- 动态照片继续复用最长边 900px、目标约 96KB 的压缩流程，直接保存在原有 `wedding_story_modules_v1` 中并随内容包迁移。
- 导入内容包时，动态照片字段只保留合法 JPEG/PNG/WebP Data URL；无效字符串会被清空，不会进入图片 `src`。
- 上传前进行整体容量预估，超过 4.5MB 建议上限时恢复原照片并提示先精简内容。

### 交互与响应式

- 编辑模式显示上传/更换入口并支持键盘、触摸；正式浏览时按钮 `aria-disabled=true`、`tabIndex=-1` 且不接收指针事件。
- 桌面照片模块宽约 436px、高 220px；390×844 下自动变为单列，照片区约 306×170px，无横向溢出。
- 触摸设备编辑时持续显示“上传照片 / 更换照片”提示，浏览时保持纯净照片画面。

### 验证

- In-app Browser 实际创建“加照片”模块，上传入口、ARIA 状态、编辑/浏览切换与响应式尺寸均通过；测试模块已用撤销功能清理。
- `modulePhotoInput` 接受 `image/*`，静态断言覆盖压缩、容量拒绝、安全导入、持久化与内容包路径。
- `index.html` 内联脚本语法检查与 `git diff --check` 通过。

## v3.11.0 — 主叙事照片直传 · 内容包扩展 · 编辑溢出修复 (2026-07-11)

### 新增

- 双轨故事的“他的照片 / 她的照片”和三张共同记忆共 5 个核心照片位，全部改为编辑模式下可直接上传与更换。
- 照片继续复用 900px / 约 96KB 的渐进压缩流程，保存到新键 `wedding_fixed_photos_v1`。
- 5 个照片位加入内容包白名单、容量计量、导入图片 Data URL 校验与失败保护；旧内容和旧存储键保持兼容。
- 正式浏览时照片位不可点击、不可聚焦，避免求婚现场误弹文件选择器；进入编辑模式后才获得键盘和触摸入口。

### UI 与响应式

- 空照片显示克制的虚线编辑边界；悬停/键盘聚焦显示“上传照片”，已有照片显示“更换照片”。
- 手机触摸设备始终显示上传/更换提示；双轨竖照片收为最多 `150×216px`，三张共同记忆改为 4:3 自适应。
- 修复编辑模式把 `#scrollHint` 从 fixed 改成 relative、导致页面横向撑宽的旧问题。

### 验证

- In-app Browser 桌面验证：浏览态 5 个照片位均 `aria-disabled=true` / `tabIndex=-1`；编辑态均切换为可操作状态。
- 390×844 验证：双轨照片不再越出文档宽度，三张共同记忆宽约 311px 且页面无横向溢出。
- 编辑模式 `scrollWidth === clientWidth`，滚动提示保持 `position: fixed`、宽约 56px。
- `index.html` 内联脚本语法检查与 `git diff --check` 通过。

## v3.10.0 — 求婚现场音乐 · 说话时自动压低 (2026-07-11)

### 新增

- 接入本地 `music/Ed Sheeran - Perfect.mp3`，增加左下角克制的唱片/声波音乐按钮；音乐只会在用户明确点击后播放，绝不自动出声。
- 音乐使用约 1.4 秒柔和渐入；再次点击会先渐出再暂停，避免突然切断。
- 进入“去往我们的下一站”后自动将音量降到 `0.16`，为现场说话留出空间；点击“我愿意”后轻轻回升到 `0.36`。
- 返回帘幕或重新进入故事时恢复叙事音量 `0.46`。

### UI 与可访问性

- 音乐按钮沿用香槟金、深色玻璃与同心唱片语言，浏览状态低透明，悬停、键盘聚焦或播放时才清晰显示。
- 按钮同步 `aria-pressed`、播放/暂停可访问名称与可见状态文字。
- 390px 手机端按钮缩为 `126×38px` 并固定在左下角，与右侧返回按钮互不冲突；减少动态效果时停止唱片和声波动画。

### 验证

- 音频资源 HTTP `200`、`Content-Type: audio/mpeg`、长度 `10638698` 字节；浏览器媒体元素 `readyState=4`。
- In-app Browser 桌面和 390×844 视觉验证通过，页面无横向溢出、按钮不遮挡核心文案。
- In-app Browser 自动化环境不提供音频输出，受控点击会拒绝实际发声；渐入/压低/回升逻辑完成静态检查，最终需要在现场使用的 Chrome/Safari 上做一次有声确认。
- `index.html` 内联脚本语法检查与 `git diff --check` 通过。

## v3.9.0 — “下一站”仪式转场 · 首页局部柔焦 · 展示态降噪 (2026-07-11)

### 新增

- 路线地图与最终问题之间新增“地图上，还有一个地方没有名字”仪式转场，让求婚问题由主动点击“去往我们的下一站”后再进入，避免情绪突然切换。
- 转场的标题、说明与提示文字均接入现有 `data-ck` 编辑器，并增加独立 `proposal-threshold` 编辑表面。
- 转场按钮同步 `aria-controls` / `aria-expanded`，点击后平滑滚动到真正的求婚终章。

### 优化

- 首页取消整屏 `backdrop-filter`，改为只在文字背后应用椭圆形局部柔焦；背景人物与环境保持清晰，文字仍由暖光和阴影维持对比度。
- 参数面板中的“文字层背景柔焦”改为“文字背后局部柔焦”，原有保存数据继续兼容。
- 正式浏览时隐藏技术版本号、弱化编辑铅笔；悬停、键盘聚焦或进入编辑模式时编辑入口恢复清晰。
- 新转场支持桌面、触摸设备与 `prefers-reduced-motion`。

### 验证

- `index.html` 内联脚本语法检查通过，`git diff --check` 通过。
- In-app Browser 桌面端验证：整屏背景不再被 DOM 层模糊；“下一站”按钮点击后 `aria-expanded=true`，转场状态生效并把终章滚动到视口顶部。
- In-app Browser 390×844 验证：转场宽度等于视口、按钮宽 260px、无横向溢出，标题、正文和按钮完整可见。

## v3.8.0 — 内容包备份 · 容量提示 · 照片体积保护 (2026-07-11)

### 新增

- 编辑工具栏新增 `导出内容包` / `导入内容包`，一次备份和恢复文字、排版、故事模块、自由框、28 张相纸、路线字号与首屏参数。
- 内容包使用 `my-wedding-content` 格式和版本号；导入只处理 8 个已知存储键。
- 导入前移除脚本、嵌入内容和全部 HTML 属性，仅允许合法图片 Data URL；替换前要求用户确认，写入失败会恢复原有内容。
- 编辑工具栏底部新增本地容量计量，3.6 MB 后黄色提醒、4.5 MB 后红色提醒。

### 优化

- 拍立得照片最长边从 `1200px` 调整为 `900px`。
- JPEG 从 `0.82` 开始逐级降低质量，目标把单张 Data URL 控制在约 `96 KB`，更适合 28 张照片保存在浏览器内。
- 照片写入失败时恢复原照片和翻面状态，并提示先导出内容包再精简照片。

### 验证

- `index.html` 5 个内联脚本语法检查通过，`git diff --check` 通过。
- In-app Browser 桌面端验证：编辑模式显示导出、导入与容量计量，当前测试内容约 `10 KB`。
- In-app Browser 390×844 验证：编辑工具栏宽度和高度均在视口内，无横向溢出。
- 页面运行错误日志为空。

## v3.7.0 — 最终求婚章节 · “我愿意”庆祝状态 (2026-07-11)

### 新增

- 首屏按钮由 `Say Yes` 调整为“翻开我们的故事”，把真正的答应时刻留到叙事结尾。
- 路线地图之后新增全屏最终求婚章节：求婚引言、求婚信、问题、答应后的标题和未来誓言。
- 点击“我愿意”后，页面进入答应状态：戒指同心光环增强、玫瑰花瓣落下，并辅以克制的香槟金庆祝粒子。
- 求婚章节 7 处文案全部接入现有 `data-ck` 编辑器，可修改、拖动、撤销并保存到 `wedding_content_v1` / `wedding_layout_v1`。
- 求婚页加入独立 `proposal` 编辑表面，可继续添加自由文本框或照片框。
- 支持 `prefers-reduced-motion`：关闭落花和过渡动画，同时保留完整信息与交互。

### 验证

- `index.html` 5 个内联脚本语法检查通过，`git diff --check` 通过。
- In-app Browser 桌面端完整流程通过：掀开婚纱 → 翻开故事 → 滚动至最终页 → 点击“我愿意”；答应状态、ARIA 状态、焦点转移与 42 片花瓣均生效。
- In-app Browser 390×844 通过：求婚页宽度不超过视口、无横向溢出，问题卡片与按钮完整可见；答应后文字清晰且没有焦点外框残留。
- 编辑模式验证通过：7 个求婚文案字段均获得 `contenteditable="true"`，答应后的文案会在编辑状态中显示。
- 页面控制台无新增运行错误。

## v3.6.1 — 首屏透明文字光晕 · 背景完整显露 (2026-07-10)

### 修改

- 移除首屏文字后方的大面积白色玻璃卡片和边框，改为中心逐渐消散的透明暖光晕。
- 标题和正文使用分层文字阴影维持明暗背景上的清晰度，不再依赖实色底板。
- 首屏文字层默认柔焦从 `15px` 降为 `7px`，背景人物与环境更容易被看见；仍可在“调节”面板中继续调整。
- 编辑文字获得焦点时只出现极淡的局部暖色反馈，避免重新形成遮挡背景的白块。

### 验证

- In-app Browser 桌面端验证：掀开婚纱后人物、花园和婚纱背景完整可见，标题、正文和 Say Yes 按钮保持清晰。
- In-app Browser 390×844 验证：文字无横向溢出，背景人物可见，标题为 `32px`、正文为 `16px`。
- 页面控制台无新增错误；内联脚本语法检查与 `git diff --check` 通过。

## v3.6.0 — 相纸状态重构 · 回忆舞台精修 · 首屏 UI 收纳 (2026-07-10)

### 修改

**1. 相纸动作收敛为三个明确入口**
- 照片区和右下箭头只负责进入回忆；左下角只负责翻面。
- 移除相纸空白区域的兜底点击，背面手写区保持专注编辑，不再有事件抢占。
- 照片区改为原生按钮，键盘与触摸设备也可完整进入回忆。

**2. 照片墙与回忆舞台的翻面状态分离**
- 墙内翻面只保存背面便签状态，不会直接打开故事。
- 每次进入回忆舞台都先从正面居中展示；仅舞台内的翻面会展开右侧故事面板。
- 舞台翻面不再反向改写照片墙相纸的正反面，关闭后焦点回到原入口。

**3. UI 与移动端细节**
- 相纸按钮扩大为可靠的触控尺寸，桌面端保留 1.6x 悬停放大，移动端不依赖 hover。
- 回忆舞台增加玻璃边框、留白和滚动容器；修复窄屏舞台的横向溢出。
- 首屏为主文案增加克制的玻璃卡片层，参数面板默认收起并提供“调节/收起”按钮。

### 验证

- `index.html` 内联脚本解析通过，`git diff --check` 通过。
- In-app Browser 本机页面验证：墙内翻面不打开回忆；进入回忆固定从正面开始；舞台翻面显示故事但不改写墙内翻面；桌面悬停得到 `scale(1.6)`。
- 390px 视口检查：页面无全局横向溢出，回忆舞台采用单列布局；窄屏容器宽度已限制在可用内容区内。

### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | UI 统一细节、相纸交互状态机、回忆舞台与移动端修复 |
| `README.md` | 更新 | 同步当前交互与版本 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.5.3 — 相纸翻面误触修正 · 相纸桌面视觉升级 (2026-07-05)

### 修复

**1. 背面手写区不再误触进入回忆**
- 左下角按钮仍然只负责翻面 / 翻回。
- 右下角淡箭头仍然负责进入回忆故事。
- 正面照片区和非编辑区域点击进入回忆。
- 背面手写文字区点击优先进入编辑状态，不再被中间点击进回忆抢走。

**2. 相纸 UI 重新打磨**
- 照片墙背景从硬斜切背景改为暖纸桌面、柔光和轻网格质感。
- 角落按钮改成更轻的玻璃感图标。
- 右下角回忆入口改为 CSS 线条箭头，不再依赖粗糙字符。
- 相纸阴影、纸面内高光和背面横线质感整体调轻。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：`APP_VERSION=3.5.3`、背面手写区点击被排除、左下翻面调用 `togglePolaroidCard`、右下入口调用 `openPolaroidMemory`、新版背景/图标 CSS 均已写入。
- Browser / Chrome / Computer Use 插件通道本轮均被 Windows 权限拦截（`CreateProcessAsUserW failed: 5`）。系统 Chrome headless 尝试生成本地截图未产出文件，因此本轮未完成真实浏览器截图/点击验证。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 修正背面手写区误触、优化照片墙背景和角落图标 |
| `README.md` | 更新 | 同步 v3.5.3 当前状态 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |
| `My Wedding Master Skill.md` | 更新 | 同步背面手写优先编辑的交互约束 |
| `Responsive Desktop & Mobile Adaptation Skill.md` | 更新 | 同步移动端触摸区约束 |

## v3.5.2 — 相纸角落箭头入口 · 中间点击进回忆 (2026-07-05)

### 修改

**1. 相纸交互区域重新划分**
- 正面和反面都保持左下角圆形按钮为翻面入口。
- 正面和反面右下角都改为若隐若现的箭头按钮，用来进入这张相纸的回忆故事。
- 点击相纸中间区域也会进入回忆故事，不再承担翻面动作。

**2. 相纸视觉细节调整**
- 原右上角 `故事` 文字按钮改成右下角箭头暗纹，hover / focus / 当前激活相纸时会更清晰。
- 编号从右下角挪到右上角，避免和回忆入口冲突。
- 相纸桌面增加一层轻微暖色光晕，让 hover 放大时更像从桌面灯光里浮起。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：`APP_VERSION=3.5.2`、右下角箭头入口、左下角翻面按钮、相纸中间点击调用 `openPolaroidMemory`、翻面按钮调用 `togglePolaroidCard`、无旧的 `故事` 文案按钮均已写入。
- Browser / Chrome 插件通道本轮被 Windows 权限拦截（`CreateProcessAsUserW failed: 5`）；系统 Chrome headless 在当前环境不返回 stdout，未完成浏览器运行态截图/点击验证。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 相纸角落交互、中心点击进回忆、桌面光晕 |
| `README.md` | 更新 | 同步 v3.5.2 当前状态 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.5.1 — 相纸翻面与故事入口分离 (2026-07-05)

### 修复

**1. 恢复照片墙里的原有翻面交互**
- 点击相纸本体或左下角翻面按钮，只在照片墙里翻到背面 / 翻回正面。
- 翻到背面后继续进入手写文字编辑状态，不再直接跳进回忆舞台。
- 正反面翻转按钮仍保持同一相对位置。

**2. 新增独立故事入口**
- 每张相纸右上角新增 `故事` 按钮。
- 点击 `故事` 才进入全屏相纸记忆舞台。
- 这样“翻面”和“进入故事回忆”变成两个清晰动作，桌面 hover 放大和移动端点击都能区分。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：`APP_VERSION=3.5.1`、墙内 `memory` 独立入口、翻面按钮调用 `togglePolaroidCard`、相纸主体点击调用墙内翻面、故事按钮调用 `openPolaroidMemory` 均已写入。
- 浏览器自动化通道本轮未得到可用输出：本地服务启动被权限审核卡住，Playwright 缺少 `playwright-core`，Chrome `--dump-dom` 在当前沙箱中静默退出。需在真实浏览器中做最终肉眼确认。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 相纸翻面和进入故事回忆拆成两个入口 |
| `README.md` | 更新 | 同步 v3.5.1 当前状态 |
| `CHECKPOINT.md` | 更新 | 同步断点、交互结论与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.5.0 — 相纸记忆展开舞台 (2026-07-04)

### 新增

**1. 点击相纸进入记忆展开模式**
- 点击照片墙里的任意相纸，会打开全屏记忆舞台。
- 背景照片墙会虚化、变暗并退到后方，当前相纸在舞台中放大展示。
- 小相纸上的翻转按钮也改为进入记忆舞台，真正的翻面动作在舞台中完成。

**2. 舞台内翻面后显示右侧故事**
- 舞台里的相纸先居中显示。
- 点击舞台相纸上的翻转按钮后，相纸翻到背面并移动到左侧。
- 右侧故事面板淡入，可编辑标题、日期、地点和正文。

**3. 故事内容保存**
- 每张相纸新增 `memoryTitle`、`memoryDate`、`memoryPlace`、`memoryStory` 字段。
- 新字段和原来的照片、背面手写文字一起保存到 `wedding_polaroids_v1`。
- 编辑右侧故事或背面文字后会保存；也可点击 `保存这段故事` 手动保存。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：`APP_VERSION=3.5.0`、记忆舞台 HTML/CSS、故事字段、旧数据补默认值、打开/翻面/保存函数均已写入。
- Chrome DevTools 协议运行态验证通过：点击第 1 张相纸打开舞台；翻面后故事面板出现；编辑标题/日期/地点/正文后保存到 `wedding_polaroids_v1`；关闭后照片墙恢复。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 新增相纸记忆展开舞台、舞台内翻面、右侧故事编辑与保存 |
| `README.md` | 更新 | 同步 v3.5.0 功能与存储字段 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.4.2 — 相纸单张锁定放大 · 批量翻面不放大 (2026-07-04)

### 修复

**1. 单张相纸翻面后锁定放大对象**
- 手动点击某张相纸后，会把这张相纸标记为当前激活相纸。
- 当前激活相纸可以保持放大；鼠标或焦点移动到其他相纸时，其他相纸不会再触发 hover/focus 放大。
- 点击另一张相纸时，激活对象会切换到新点击的相纸。

**2. 一键翻面后不再放大任何相纸**
- `一键翻面` 和 `全部正面` 会清除相纸激活状态。
- 批量翻面不再自动聚焦第一张相纸，因此第一张不会因为 `focus-within` 被放大。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：`APP_VERSION=3.4.2`、active 锁定 CSS、inactive hover 覆盖、单张翻面设置 active、批量翻面清除 active、批量翻面不再聚焦第一张均已写入。
- Chrome DevTools 协议运行态验证通过：第 1 张手动翻面后，第 2 张获得焦点/模拟 hover 仍保持 `scale(1)`；点击 `一键翻面` 后 active 数量为 0，前 6 张抽样相纸均未放大，28 张均为背面。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 单张 active 放大锁、inactive hover 覆盖、批量翻面清除 active 且不聚焦 |
| `README.md` | 更新 | 同步 v3.4.2 当前状态 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.4.1 — 相纸背面不全选 · 背面禁用上传误触 (2026-07-04)

### 修复

**1. 翻到背面后不再全选文字**
- 保留翻到背面后自动聚焦手写文字区的行为，方便继续编辑。
- 移除默认提示文字自动全选逻辑，光标只放到文字末尾，文字选择交给用户自己操作。

**2. 背面状态不再误触上传照片**
- `.is-flipped` 状态下隐藏并禁用正面的上传按钮指针事件。
- 点击处理里增加背面状态保护：相纸已经翻到背面时，即使事件命中正面上传按钮，也不会打开文件选择框。
- 文件输入变更处理也增加背面状态兜底，避免异常路径写入照片。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：`APP_VERSION=3.4.1`、背面禁用上传 CSS、上传点击保护、文件输入兜底、翻面聚焦不全选均已写入。
- Chrome DevTools 协议运行态验证通过：翻面后背面文字区获得焦点但选区为空；背面状态下模拟点击正面上传按钮不会触发文件输入。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 翻面后不全选文字、背面状态禁用上传误触 |
| `README.md` | 更新 | 同步 v3.4.1 当前状态 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.4.0 — 相纸悬停即时翻面 · 1.6x 放大默认值 · 背面字号可调 (2026-07-03)

### 修改

**1. 修复悬停状态下点击翻面不即时的问题**
- 相纸结构拆成两层：`.polaroid-card-visual` 只负责 hover 放大，`.polaroid-card-inner` 只负责正反面 `rotateY` 翻转。
- 鼠标停在哪张相纸上，当前那一面会持续放大；此时点击翻面按钮或相纸主体，会立刻切到另一面，不再需要移开鼠标才看到翻面结果。
- 翻到背面后自动进入手写文字区，默认提示 `写给这张照片的话` 会被选中，可直接输入覆盖。

**2. 放大倍数与背面字号保存**
- 悬停放大默认值写成 `1.6x`，同时保留 `放大` 滑块，可在 `1.05x` 到 `1.80x` 间实时调节。
- `保存放大倍数` 会把当前值写入 `wedding_polaroid_layout_v1.hoverScale`，后续可读取该值再写死为新的默认值。
- 新增 `背面字` 滑块和 `保存字大小` 按钮，保存到 `wedding_polaroid_layout_v1.noteFontSize`。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：`APP_VERSION=3.4.0`、hover 视觉层、翻面旋转层、`polaroidHoverScaleInput`、`polaroidSaveScaleBtn`、`hoverScale` 存储、`polaroidNoteSizeInput`、`polaroidSaveNoteSizeBtn`、`noteFontSize` 存储均已写入。
- Chrome DevTools 协议运行态验证通过：悬停/聚焦状态下点击第 1 张相纸后 `is-flipped=true`，视觉层继续放大，内层立即旋转；放大倍数与背面字号保存后写入 localStorage。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 相纸 hover/flip 分层、1.6x 默认放大、放大倍数保存、背面字号保存、背面快速聚焦 |
| `README.md` | 更新 | 同步 v3.4.0 功能与存储字段 |
| `CHECKPOINT.md` | 更新 | 同步断点、验证记录和写死流程 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.3.0 — 相纸背面写字状态 · 悬停放大倍数可调 (2026-07-03)

### 修改

**1. 翻到背面自动进入写字状态**
- 单张相纸点击翻到背面后，会自动聚焦背面的手写文字区。
- 如果背面还是默认提示 `写给这张照片的话`，会自动选中这段提示文字，用户可直接输入替换。
- 一键翻面后会自动聚焦第一张背面文字区，方便立刻开始写。

**2. 相纸悬停放大倍数可调**
- 拍立得控制区新增 `放大` 滑块，范围 `1.05x` 到 `1.60x`，实时调整 hover 放大倍数。
- 新增 `保存放大倍数` 按钮，保存到 `wedding_polaroid_layout_v1.hoverScale`。
- 后续要写死最终效果时，可读取 `wedding_polaroid_layout_v1.hoverScale`，再同步到 `DEFAULT_POLAROID_HOVER_SCALE` 和 CSS 默认 `--polaroid-hover-scale`。

### 验证

- `index.html` 内联脚本语法检查通过。
- 静态断言通过：版本号、放大倍数滑块、保存按钮、`hoverScale` 存储、背面自动聚焦和默认文字选中逻辑均已写入。
- Chrome DevTools 协议运行态验证通过：点击第 1 张相纸翻面后，背面 note 成为 activeElement，默认提示文字被选中；放大倍数调到 `1.42` 后 CSS 变量生效，保存后 localStorage 中 `hoverScale` 为 `1.42`。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 背面自动聚焦、默认文字选中、hover 放大倍数滑块与保存 |
| `README.md` | 更新 | 同步 v3.3.0 功能与存储字段 |
| `CHECKPOINT.md` | 更新 | 同步断点、验证记录和写死流程 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.2.0 — 中国地图轮廓 · 地名字号调节 · 相纸批量翻面 (2026-07-03)

### 修改

**1. 地图轮廓与字号调节**
- 中国地图轮廓从圆润占位轮廓改为参考 Natural Earth 公共领域地图数据的简化轮廓，并加入海南、台湾等岛屿轮廓。
- 地图轮廓初始隐藏，8 个地址点全部显示后进入 `is-map-complete`，中国轮廓浮现为背景。
- 新增 `地名字号` 滑块，可实时调节地图地名文字大小，保存到 `wedding_route_label_size_v1`。

**2. 拍立得批量控制与 hover 稳定**
- 相纸 hover 放大幅度提升到桌面端 `1.28`，更明显地从桌面上跳出来。
- 新增 `一键翻面` 和 `全部正面` 按钮，批量更新 28 张相纸状态并保存到 `wedding_polaroids_v1`。
- 修复 hover 闪烁：v3.1.0 外层卡片位移会让鼠标在边缘反复进入/离开 hover；v3.2.0 改为外层命中区稳定，内层视觉放大，并增加隐形 hover 缓冲区。

### 验证

- `index.html` 内联脚本语法检查通过。
- Headless Chrome `--dump-dom` 验证通过：新增按钮、字号滑块、中国轮廓在运行页面 DOM 中出现。
- Chrome DevTools 协议运行态验证通过：28 张相纸存在，一键翻面/全部正面生效，字号滑块设置到 `34px` 生效，8 个地图点显示后完成态为 true，轮廓 opacity 过渡到 `0.9`，桌面 hover scale 为 `1.28`。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 中国地图轮廓、地名字号滑块、批量翻面/正面、hover 防闪烁 |
| `README.md` | 更新 | 同步 v3.2.0 功能与存储键 |
| `CHECKPOINT.md` | 更新 | 同步断点、验证记录和 bug 分析 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.1.0 — 地图放大 · 点位累计显名 · 拍立得悬停弹出 (2026-07-03)

### 修改

**1. 路线地图交互增强**
- 地图主体最大宽度从 `780px` 提升到 `1060px`，页面中间地图区域同步放大。
- 地址点改为悬停、聚焦或点击后累计显示地名，不再切换到新点时隐藏旧点。
- 全部地址点都显示后，路线页进入 `is-map-complete` 完成态，地图轮廓和内部轮廓线浮现为背景。

**2. 拍立得相纸交互增强**
- 相纸悬停或内部控件聚焦时会抬升、放大，并提高层级，像从桌面上跳出来。
- 正面和背面新增同一位置的翻转按钮；翻过去和翻回来都点同一个相对位置。
- 保留原来的点击相纸主体翻面逻辑，同时避开上传按钮、文件输入和背面文字编辑区。

### 验证

- `index.html` 内联脚本语法检查通过（inline-script-1 OK）。
- 本地服务器 `http://127.0.0.1:8090/index.html` 返回 200。
- 静态断言通过：版本号、拍立得悬停放大 CSS、正反面同位置翻转按钮、地图累计显名、完成态轮廓、地图宽度提升均已写入。
- Browser / headless Chrome 自动化本轮被 Windows 沙箱权限审核阻断，未完成视觉自动化实测。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 地图放大、点位累计显示、完成态轮廓、拍立得悬停弹出和同位翻转 |
| `README.md` | 更新 | 同步 v3.1.0 功能说明 |
| `CHECKPOINT.md` | 更新 | 同步断点、验证记录和会话记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v3.0.0 — 拍立得照片墙 · 地图合页 · 地址点击显名 (2026-07-03)

### 新增

**1. 28 张拍立得照片墙**
- 新增独立照片墙页面，位于故事模块之后、路线地图之前。
- 自动生成 28 张拍立得相纸卡片，默认像随意散落在桌面上一样排列。
- 每张卡片右下角显示 1-28 编号，便于替换照片时识别。
- 每张卡片可上传照片，图片会在浏览器内压缩后保存到 `wedding_polaroids_v1`。
- 点击卡片会慢速翻到背面，背面可直接输入文字，并使用中文手写体风格展示。

**2. 拍立得一键排列与间距保存**
- 页面底部新增 `一键排列`、`随意摆放`、`间距` 滑块和 `保存间距`。
- 一键排列后按 1-28 顺序排版，整体宽度约占屏幕 80%。
- 间距设置保存到 `wedding_polaroid_layout_v1`，后续可读取该值并写死为最终满意间距。

**3. 路线地图双页合并**
- 原 `route-points` / `route-labels` 两页合并为单个 `route-map` 页面。
- 坐标点保持固定，只有路线光带在地址之间慢速流转。
- 地名默认隐藏，鼠标悬停、键盘聚焦或点击任意地址点时显示地名。
- 旧自由框页面 id 会从 `route-points` / `route-labels` 自动迁移到 `route-map`。

### 验证

- 内联脚本语法检查通过。
- 本地服务器 `http://127.0.0.1:8090/index.html` 返回 200。
- 使用系统 Chrome + Playwright 验证通过：拍立得数量 28、一键排列生效、间距实时更新、保存间距写入 `{"arranged":true,"gap":24}`、第 1 张卡片可翻面、地图只剩 1 个 route section、点击地图地址点后地名可见。
- 当前沙箱仍会因外部 CDN 受限出现预期的 `THREE is not defined`，与 v2.7-v2.9 验证备注一致，不属于本次新增逻辑错误。

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 新增拍立得页面、照片上传/翻面/手写背面、地图合页与点击显名 |
| `README.md` | 更新 | 同步 v3.0.0 功能说明与存储键 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

## v2.9.0 — 自由框页面融合 · 浏览态淡入淡出 (2026-06-28)

### 修复

**1. 新增内容滑动时突兀**
- 自由文本框/照片框不再直接作为高层级浮贴压在页面上
- 每个可编辑页面会创建自己的 `.free-item-layer`，新增内容渲染到所在页面的内容层中
- 浏览态自由框 `pointer-events:none`，不拦截页面原有交互
- 自由框随所在页面进入/离开视口淡入淡出，避免滑动时突然覆盖下一页
- 编辑态临时抬高 `.free-item-layer`，确保拖动、色板、复制、删除仍然可点

### 视觉调整

- 自由框继承所在页面的文字颜色变量，透明背景时更像页面旁白
- 照片框浏览态改为更轻的页面边框，编辑模式才显示明显虚线
- 有背景色的自由框增加轻微毛玻璃和阴影，保持和页面质感一致

### 验证

- 5 个内联脚本语法检查通过
- Playwright 验证通过：自由框位于 `.free-item-layer`，当前页显示，离开页面后淡出到 opacity 0
- Playwright 验证通过：浏览态自由框不拦截点击；编辑态仍可拖动和使用色板
- Playwright 验证通过：平行宇宙 sticky 页仍保持位置稳定
- Playwright 移动端验证通过：色板可点，退出编辑后保留页面融合展示

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | `.free-item-layer` 页面内容层、滚动淡入淡出、浏览态不拦截点击 |
| `README.md` | 更新 | 同步 v2.9.0 功能说明 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

---

## v2.8.0 — 自由框锚定 · 透明背景 · 背景色切换 (2026-06-27)

### 修复

**1. 自由框随页面移动**
- `parallel` 平行宇宙页的自由框改为锚定 `.dual-track-stage`，匹配该页 sticky 视觉层，不再在滚动时从页面上漂走
- 普通页面自由框继续锚定所在 section，滚动时随页面同步移动
- 连续新增自由框时自动错开，避免后添加的照片框挡住前一个文本框控制点
- 编辑模式下自动隐藏参数控制面板，避免移动端覆盖编辑工具条和自由框色板

### 新增功能

**1. 自由框背景色**
- 文本框和照片框默认背景改为透明，保留页面背景可见
- 每个自由框控制条新增 `色` 按钮，可从色块中选择透明、柔白、米白、玫瑰、青蓝、深色背景
- 背景色保存到 `wedding_free_items_v1`，刷新后自动恢复

### 验证

- 5 个内联脚本语法检查通过
- Playwright 验证通过：平行宇宙页自由框跟随 sticky 页面，不随滚动漂移
- Playwright 验证通过：路线页自由框随普通页面滚动
- Playwright 验证通过：文本框/照片框默认透明，背景色可切换、保存并刷新恢复
- Playwright 移动端验证通过：参数面板不再遮挡编辑工具条，色板在 390px 宽视口内可点击

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 自由框锚定、背景色面板、透明默认背景、连续新增错开 |
| `README.md` | 更新 | 同步 v2.8.0 功能说明 |
| `CHECKPOINT.md` | 更新 | 同步断点与验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

---

## v2.7.0 — 自由编辑增强 · 撤销保存 · 防卡顿输入 (2026-06-27)

### 新增功能

**1. 工具条完善**
- 右上角编辑工具条新增 `加文本框`、`加照片框`、`撤销`、`保存`
- 工具条保持两列固定面板，模块按钮和编辑按钮完整可见

**2. 每页自由新增文本框/照片框**
- 根据当前视口所在页面新增自由浮层对象
- 支持拖动、复制、删除、直接编辑文字/照片说明
- 地图两页可分别保存到 `route-points` 和 `route-labels`
- 新增存储键：`wedding_free_items_v1`

**3. 撤销与显式保存**
- `撤销` 可回退文字内容、文字位置、故事模块、自由文本框/照片框状态
- `保存` 会完整写入当前编辑状态，下次打开自动读取
- `Ctrl+S` 在编辑模式中触发同一套保存逻辑

### 性能优化

- 静态文案输入从“每次自动扫描全部可编辑节点”改为“只保存正在修改的脏字段”
- 故事模块输入从“每次重建全部模块”改为“只更新当前字段”
- 完整扫描/完整写入只在手动保存或退出编辑模式时执行，降低 contenteditable 编辑卡顿概率

### 验证

- 5 个内联脚本语法检查通过
- Playwright 验证通过：工具条完整、自由文本框保存/刷新恢复、撤销新增照片框
- Playwright 验证通过：地图两页新增对象分别保存到正确页面

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 编辑工具条、自由页面对象、撤销栈、防卡顿保存 |
| `README.md` | 更新 | 同步 v2.7.0 功能说明 |
| `CHECKPOINT.md` | 更新 | 同步断点、存储键、验证记录 |
| `CHANGELOG.md` | 更新 | 本条目 |

---

## v2.1.0 — 网眼白度 · 掀开特效 · 重返按钮 · 参数持久化 (2026-05-31)

### 新增功能

**1. 白纱面料视觉升级**
- **`hexFill` 参数**（0–180，默认 55）：控制六边形纹理背景灰度
  - 0 = 纯黑底，网眼之间完全透明（原版效果）
  - 180 = 近白底，网眼之间接近不透明（接近真实婚纱质感）
- **`hexLineWidth` 参数**（2.0–10.0，默认 5.0）：六边形框线粗细
  - 线越粗白纱视觉效果越明显
- 两项参数修改后实时重新生成 Canvas 纹理并更新材质

**2. 掀开粒子特效**
- **Canvas Sparkle 钻石粒子**：掀开瞬间在鼠标位置爆发 60 个旋转钻石形粒子
  - 自带重力、衰减、光晕
  - 粒子颜色混合纯白 `#ffffff` 和浪漫粉 `#f4c2c2`
- **Canvas-Confetti 双重爆发**：80 个彩屑同步发射
- **DOM 层白光闪过**：GSAP 内阴影动画 `inset box-shadow`

**3. 重返帘幕按钮**
- 进入滚动内容后，右下角出现圆形悬浮按钮（↑ 箭头图标）
- 悬停显示 "重返帘幕" 提示文字
- 点击后：
  - 隐藏滚动内容，显示三图层
  - 重新初始化布料物理（`initPhysics`）
  - 杀死并重建所有 ScrollTrigger
  - 平滑滚动回顶部

**4. 参数持久化（Save / Load）**
- **💾 保存设置按钮**：控制面板底部，点击保存全部 17 个参数到 `localStorage`
  - 保存后按钮变绿 "✓ 已保存" 1.8 秒
  - Toast 通知： "✨ 参数已保存到本地，下次自动读取"
- **自动加载**：页面初始化时从 `localStorage` 读取，合并到默认值（新参数用默认值）
  - 加载成功 Toast： "📋 已加载上次保存的参数"
- 存储键名：`wedding_veil_config_v2`

### 参数总览 (v2.1.0)

| 分组 | 参数 | 默认值 | 范围 |
|------|------|--------|------|
| 六边形纹理 | hexFill | 55 | 0–180 |
| | hexLineWidth | 5.0 | 2.0–10.0 |
| | tiling | 448 | 10–600 |
| 面料 | opacity | 0.86 | 0.3–1.0 |
| | blur | 0.6px | 0–2.5px |
| | dragRadius | 7.0 | 1.0–10.0 |
| | stiffness | 0.35 | 0.1–1 |
| 物理 | flightForce | 1.8 | 0.5–4.0 |
| | threshold | 120 | 50–250 |
| | damping | 0.98 | 0.90–0.99 |
| | gravity | 0.014 | 0–0.04 |
| | wind | 0.015 | 0–0.04 |
| 视频/图层 | videoOpacity | 0.92 | 0.3–1.0 |
| | edgeBlur | 0px | 0–40px |
| | domBlur | 15px | 0–30px |

### 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 更新 | 四项改进 + 17 参数控制 + localStorage + Sparkle 引擎 |
| `README.md` | 更新 | v2.1.0 参数表 + 功能列表 |
| `CHANGELOG.md` | 更新 | 本条目 |

---

## v2.0.0 — 三图层 Z-Index 架构 · 六边形网纱布料 · 视频背景 (2026-05-31)

### 重大架构升级

**三图层全屏堆叠架构** (参考 `wedding_architecture.md` 核心设计)：

```
Layer 3 (z:3): Three.js WebGL 正六边形网纱布料 — 顶层交互
Layer 2 (z:2): DOM Hero Card — 毛玻璃模糊 + 情话编辑 + Say Yes
Layer 1 (z:1): 视频背景 — video/1.mp4 全屏播放 + 边缘模糊遮罩
```

### Layer 3 — 六边形网纱布料 (全新实现)

- **程序化正六边形纹理**：利用 Canvas API + 三角函数按 60° 步进绘制密铺六边形线框，作为 `alphaMap` 透明通道贴图
- **双通道渲染防穿模**：`BackSide` (renderOrder=1) + `FrontSide` (renderOrder=2)，强制 GPU 先画背面再画正面
- **质点-弹簧模型**：
  - 结构弹簧（上下左右相邻）
  - 剪切弹簧（对角线相连）
  - 抗弯曲弹簧（隔点连接，防止尖锐三角形死角）
- **Verlet 积分物理**：`NewPosition = Current + (Current - Previous) * Damping + Force * dt²`
- **空间防穿模排斥力**：非相邻质点间距 < 1.4 世界单位时施加微弱排斥力
- **高斯衰减拖拽**：`weight = (1 - dist/radius)²`，实现柔软无死角面域提拉

### Layer 2 — DOM Hero Card

- `backdrop-filter: blur()` 毛玻璃效果，视频背景透出
- `contenteditable="true"` 可编辑情话区
- "Say Yes" 呼吸光晕按钮

### Layer 1 — 视频背景

- `video/1.mp4` 全屏 `object-fit: cover` 播放
- 边缘模糊遮罩：`radial-gradient` mask + `backdrop-filter: blur()`，柔化宽高比不匹配时的边缘
- 参数可调节：透明度、边缘模糊强度

### 参数控制器

15 个实时调节滑块（左上角面板），分类为：

| 分类 | 参数 |
|------|------|
| 面料 | opacity, blur, tiling, dragRadius, stiffness |
| 物理 | flightForce, threshold, damping, gravity, wind |
| 视频/图层 | videoOpacity, edgeBlur, domBlur |

### Say Yes → 滚动内容过渡

- 点击 "Say Yes" → 白色闪光过渡 → 所有固定图层隐藏
- 向下滚动内容激活：双轨视差 → 相遇粒子 → 合并记忆
- GSAP ScrollTrigger 在过渡完成后自动 refresh

### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 重写 | 三图层集成 + 15 参数控制器 + 全 JS/CSS 内联 |
| `wedding_architecture.md` | 新增 | 3D 高定婚纱多图层交互系统核心架构文档 |
| `video/1.mp4` | 新增 | Layer 1 背景视频素材 |
| `code.html` | 保留 | 独立参考实现（含流体渐变背景版） |
| `README.md` | 更新 | v2.0.0 架构图 + 参数表 + 项目结构 |
| `CHANGELOG.md` | 更新 | 本文件 |

### 页面流 (v2.0.0)

```
[视频背景 + 网纱布料 + Hero Card] ──拖拽掀开──▶ [Hero Card] ──Say Yes──▶ [双轨视差] ──▶ [相遇点] ──▶ [合并轨道]
```

### 技术栈

- Three.js r160 (WebGL 2.0) — 双通道渲染 + 程序化纹理
- GSAP 3.12.5 + ScrollTrigger — 过渡动画 + 滚动视差
- canvas-confetti 1.9.3 — 相遇粒子
- 纯手写 Verlet 物理引擎 — 零外部物理库依赖
- Google Fonts: Playfair Display + Cormorant Garamond + Noto Serif SC

---

## v1.1.1 — 物理手感修复 · 掀开桌布体验 (2026-05-30)

### Bug 修复
- **THREE.RGBFormat 错误**：移除 r160 已废弃的格式常量，修复脚本初始化崩溃
- **透明度无效**：`MeshBasicMaterial` 增加 `transparent: true`，滑出动画 opacity 正常生效
- **布料过刚**：约束迭代从 3→1，布料从塑料板变为柔软织物
- **鼠标力度不足**：MOUSE_FORCE 0.18→0.55，拖拽跟手；力场半径 0.55→0.6，褶皱更真实
- **Fallback 纹理冻结**：修复更新条件（帧计数器取模 → 每 3 帧刷新）
- **时间步标准化**：引入固定步长 `FIXED_DT = 1/60`，重力用 `-9.8 m/s²`，物理与帧率解耦
- **角落方向丢失**：滑出前保存 `savedDragCorner` 用于确定滑动方向

### 体验优化
- **桌布掀开手感**：抓取布料底角向外拉，布料产生真实褶皱和跟随，超过 40% 面积位移后自动滑出
- **拖拽提示**：拖拽开始时隐藏底部提示文字，松手后未触发则恢复
- **切后台保护**：大帧跳（>200ms）自动截断，防止布料爆炸
- **移动端触摸**：增加 `touchcancel` 事件处理

---

## v1.1.0 — Three.js 布料模拟 · 转身新娘与命运画布 (2026-05-30)

### 重大升级：Hero Section 重构

**Three.js 布料物理引擎** (`js/app.js`)：
- **WebGL 布料网格**：60×60 PlaneGeometry（3721 顶点），顶点级 Verlet 积分物理
- **视频纹理映射**：`THREE.VideoTexture` 将新娘转身视频实时映射到布料表面，视频随网格变形同步弯曲
- **约束系统**：结构约束（水平+垂直）+ 剪切约束（双对角线），每帧 3 次迭代求解
- **物理参数**：重力 -0.0004、阻尼 0.98、拖拽力场二次衰减
- **Fallback 纹理**：无视频时自动使用 Canvas 2D 程序化纹理（婚纱剪影 + 微光波动）

**拖拽交互**：
- Raycaster 精准检测布料左下角/右下角抓取（判定半径 0.22 世界单位）
- 拖拽时力场影响周围顶点（二次衰减 falloff），产生逼真布料褶皱
- 实时顶点位移追踪，超过 40% 顶点位移 > 0.08 单位时触发

**滑出过渡 & Hero Card 揭示**：
- GSAP Timeline：布料沿拖拽方向平移 + Y轴旋转 + 透明度衰减（0.9s power3.in）
- 画布容器淡出后显露底层 Hero Card
- Hero 文本 contenteditable 可编辑、呼吸光晕 Say Yes 按钮

### 新增/修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `js/app.js` | 新增 | Three.js 布料物理引擎 + 交互 + 过渡 |
| `index.html` | 重构 | 三层页面结构 (cloth → hero → dual-track) |
| `css/landing.css` | 重写 | 布料容器 + Hero Card 排版 + 呼吸光晕按钮 |
| `js/config.js` | 更新 | 版本 → 1.1.0 |
| `PROMPT_GUIDE.md` | 更新 | 新增 §1 Three.js 布料规格、§2 Hero Card 规格、§6 素材路径 |
| `assets/video/` | 新增 | 视频素材目录 |
| `assets/images/` | 新增 | 图片素材目录 |

---

## v0.3.0 — 首页 Landing Page & 画布拖拽 (2026-05-30)

### 新增
- **首页 Landing 覆盖层**：全屏暗色背景 `#0a0a0a`，作为整个网站的入口
- **Canvas 新娘剪影**：程序化绘制优雅婚纱剪影（头纱、胸衣、蓬松裙摆、腰带装饰），自适应 DPR 高清渲染
- **画布拖拽交互**：画布初始定位右下角，支持鼠标拖拽 + 触摸拖拽，发光提示环
- **解锁过渡**：画布中心拖至视口中心 22% 阈值内自动触发，白色闪光 + 首页淡出
- **"SAY YES" 按钮**：Playfair Display 衬线字体，`#c0392b` 古典红，悬停填充动画
- **排版设计**：引入 Google Fonts，标题 italic 衬线体
- **新增文件**：`css/landing.css`、`js/landing.js`、`CONTENT_TEMPLATE.md`

---

## v0.2.0 — 模块化拆分 & 版本标识 (2026-05-30)

- **单文件 → 模块化**：`index.html` 瘦身为入口文件，CSS/JS 各自独立
- **CSS 模块**（`css/`）：reset, design-system, dual-track, meeting-point, merged-track, version-badge
- **JS 模块**（`js/`）：config, main, parallax, meeting, merged
- **页面版本标识**：右下角固定显示版本号，亮/暗背景自适应切换

---

## v0.1.0 — 双轨视差骨架搭建 (2026-05-30)

- 完整 HTML/CSS/JS 骨架
- **双轨布局**：GSAP ScrollTrigger 异步视差 (左轨 -45%, 右轨 -70%)
- **相遇点过渡**：canvas-confetti 四次粒子爆发
- **合并轨道**：记忆卡片 ScrollTrigger 逐个淡入
- CDN 依赖：GSAP 3.12.5 + ScrollTrigger + canvas-confetti 1.9.3
